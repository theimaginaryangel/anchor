/**
 * NextAuth Configuration
 *
 * Sets up NextAuth.js v5 with the Microsoft Entra ID provider.
 * Reads app roles (admin, viewer) from the Entra ID token and
 * attaches them to the session so route handlers can check
 * permissions.
 *
 * Implemented in Phase 1.
 *
 * Dependencies: next-auth@beta
 */

// NextAuth config goes here. Implemented in Phase 1.
export const authConfig = {};
